# Everything in lib/ and tools/ is stdlib except one call: lib/typesafe_client.py talks to
# the TypeSafe API over plain HTTP via `requests`. Pinned loosely; nothing here depends on a
# specific requests release.
requests>=2.28,<3
